
// export const getCurrentUserId = (token: string | null) => {
//     if (!token) return null;
//     try {
//       const decodedToken = JSON.parse(atob(token.split('.')[1])); 
//       return decodedToken.userId; 
//     } catch (e) {
//       console.error('Failed to decode token', e);
//       return null;
//     }
//   };
  